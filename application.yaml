management:
  endpoints:
    web:
      exposure:
        include: '*'

eureka:
  client:
    serviceUrl:
      defaultZone: ${EUREKA_SERVICE_URI:http://localhost:9010/eureka}
  instance:
    preferIpAddress: true